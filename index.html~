---
layout: default
title: Banana on the Wall
---

<div id="home">
	{% for post in paginator.posts %}
	<div id="post">
		<h2><a href="{{ post.url }}">{{ post.title }}</a></h2>
		<p class="meta">{{ post.date | date_to_string }}</p>
		{{ post.excerpt }}
	</div>
	{% endfor %}
</div>